#!/usr/bin/env bash
# event: PreToolUse
# matcher: Bash
# description: What this hook checks and when it runs.
set -euo pipefail

# A hook reads the event JSON on stdin and exits non-zero to block.
exit 0
