- **High — `ui/src/stores/marketplaces.ts:125-143`, `crates/app/src/marketplaces.rs:256-281`: Skills.sh “Install” only subscribes; it never installs the selected skill.** `skillssh-search.tsx:71-79` sends a skills.sh URL into `SubscribeDialog`, and core returns the selected package in `SubscribeOutcome.lead`, but the store ignores `response.data.lead` and merely reloads marketplaces. Clicking Install therefore leaves the skill uninstalled, contrary to §3.2/§5.7’s required “subscribe + install, one previewed plan.” No UI or integration test exercises the result of this click.

- **High — `crates/core/src/registry.rs:32-71`: response-size protection does not cap HTTP headers, so a hostile endpoint can exhaust app memory before parsing.** `--max-filesize 16000000` limits curl’s response body, but `curl -i` includes headers in stdout and `Hardened::run` drains stdout into an unbounded `Vec<u8>`. A compromised kendex.ai or MITM’d skills.sh server can stream an arbitrarily large header block; the app retains it all before `parse_http_response` runs, wedging or killing the process. Tests bypass `CurlFetch`, so none pin total-response/header limits, non-UTF-8 headers, folded headers, or oversized bodies.

- **Medium — `crates/core/src/registry/cache.rs:69-83`: body and metadata are committed separately, allowing an ETag/body mismatch to survive a crash.** Starting from body A/meta ETag A, a 200 for body B writes `index.json` first; a crash before `index.meta.json` leaves body B paired with ETag A. Next refresh sends A, and a 304 causes lines 52-67 to bless and serve body B as current even though the server only confirmed A. Store body plus ETag/fetch time as one atomic generation, or make metadata identify and verify its body. Existing cache tests cover clean 200/304 paths only, not interrupted writes or divergent pairs.

- **Medium — `crates/core/src/registry/cache.rs:113-118`, `crates/core/src/fs.rs:51-55`: on-disk cache bypasses remote size caps and is read wholly before validation.** A corrupt or locally replaced `index.json` can be arbitrarily large; every Community load synchronously `read_to_string`s the entire file and only then parses it. This can repeatedly stall or OOM the app even while online, because cache reading happens before network fallback. Bound cache-file reads to the same total limit as network responses and discard oversized files. No test covers oversized or hostile cache files.

- **Medium — `crates/core/src/registry/skillssh.rs:73-92`, `ui/src/components/marketplaces/skillssh-search.tsx:71-79`: adapter accepts skill names that its own install URL cannot parse.** Validation caps length but permits `/`, whitespace, control characters, `..`, and arbitrary URL-significant characters in `hit.name`. A hit named `foo/bar` is displayed with an enabled Install button, then produces `https://skills.sh/owner/repo/foo/bar`; `source_ref::parse_skills_sh_url` refuses it because it has four path segments. Other accepted names can similarly fail `check_component`. Validate the skill as one safe URL/path component before returning the hit. Current adapter test covers malformed `source` only.

- **Low — `ui/src/components/marketplaces/community-tab.tsx:84-159`: first load renders an empty bordered list instead of a designed loading state.** While `directory === null`, `loading === true`, and no error exists, `rows` is empty and the component renders the list container with no rows; only the small refresh icon spins. On a slow first fetch the tab appears blank, undermining the stated never-blank behavior. Add a concrete loading state and pin it with a component test; no Community UI tests exist.

REVIEW-COMPLETE